class Solution(object):
    def majorityElement(self, nums):
        d = {}
        for i in nums:
            d[i] = d.get(i, 0) + 1
        n = len(nums) // 2
        for key, value in d.items():
            if value > n:
                return key
